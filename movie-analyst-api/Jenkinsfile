node {
    stage('Checkout scm') {
        checkout scm
    }

    stage('Run test') {
        docker.image('node') { c ->
            echo 'Performing tests'
            sh 'npm install --only=dev'
            sh 'npm test'
        }
    }

    stage('Build docker image') {
        echo "Not yet"
    }
}